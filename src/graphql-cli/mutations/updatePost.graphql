mutation UpdatePost($postInput: PostInput!, $updatePostId: ID!) {
  updatePost(postInput: $postInput, id: $updatePostId) {
    code
    success
    message
    errors {
      field
      message
    }
  }
}
