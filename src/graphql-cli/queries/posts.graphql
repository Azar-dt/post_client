query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    totalPosts
    cursor
    hasmore
    paginatedPosts {
      id
      title
      text
      createdAt
      userId
      user {
        email
        username
        id
      }
      textSnippet
    }
  }
}
